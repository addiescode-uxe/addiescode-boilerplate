import React from 'react';
import { Layout } from 'antd';
const { Footer } = Layout;

const IncFooter = () => {
  return <Footer style={{ textAlign: 'center' }}>React fe project</Footer>;
};

export default IncFooter;
